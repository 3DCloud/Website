import { DjsDateFormatPipe } from './djs-date-format.pipe';

describe('DjsDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DjsDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

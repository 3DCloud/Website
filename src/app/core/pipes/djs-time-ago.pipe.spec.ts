import { DjsTimeAgoPipe } from './djs-time-ago.pipe';

describe('DjsTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new DjsTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});

query GetPrints {
  prints {
    id
    uploadedFile {
      id
      userId
      filename
      byteSize
    }
    printer {
      id
      name
    }
    status
    cancellationReason {
      name
      description
    }
    cancellationReasonDetails
    createdAt
    updatedAt
    startedAt
    completedAt
  }
}

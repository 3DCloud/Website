query GetPrints {
  prints {
    id
    uploadedFile {
      id
      userId
      filename
      byteSize
    }
    printer {
      id
      name
    }
    status
    createdAt
    updatedAt
    startedAt
    completedAt
  }
}

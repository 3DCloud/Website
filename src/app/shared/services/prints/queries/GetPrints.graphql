query GetPrints {
  prints {
    id
    uploadedFile {
      id
      filename
      byteSize
    }
    printer {
      id
      name
    }
    status
    createdAt
    updatedAt
    startedAt
    completedAt
  }
}

query ($clientId: ID!) {
  client(id: $clientId) {
    id
    name
    authorized
    createdAt
    updatedAt
    devices {
      id
      path
      serialNumber
      lastSeen
      createdAt
      updatedAt
      printer {
        id
      }
    }
    printers {
      id
      name
      deviceId
    }
  }
}

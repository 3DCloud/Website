query {
  printers {
    id
    printerDefinition {
      name
      thumbnail {
        url
      }
    }
    printerExtruders {
      extruderIndex
      materialColor {
        id
        name
        color
        material {
          id
          name
        }
      }
    }
    name
    state
    progress
    deviceId
    createdAt
    updatedAt
  }
}

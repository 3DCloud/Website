query {
  printers {
    id
    name
    state
    deviceId
    createdAt
    updatedAt
    device {
      id
      hardwareIdentifier
      client {
        id
      }
    }
    printerDefinition {
      id
      name
    }
  }
}

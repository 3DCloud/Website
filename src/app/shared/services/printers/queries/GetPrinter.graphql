query ($id: ID!) {
  printer(id: $id) {
    id
    name
    state
    deviceId
    createdAt
    updatedAt
    currentPrint {
      id
      userId
    }
    printerDefinition {
      id
      name
    }
  }
}

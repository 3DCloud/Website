query ($id: ID!) {
  printer(id: $id) {
    id
    name
    deviceId
    createdAt
    updatedAt
    currentPrint {
      id
      userId
    }
    printerDefinition {
      id
      name
    }
  }
}
